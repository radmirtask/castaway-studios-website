# netlify.toml

[build]
  # Install & build your Astro site
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  # Environment variables for your contact API
  CONTACT_EMAIL = "Contact@castaway.studio"
  SMTP_HOST     = "smtp.castaway.studio"
  SMTP_PORT     = "587"
  SMTP_USER     = "Contact@castaway.studio"
  SMTP_PASS     = "Castaway@123"
